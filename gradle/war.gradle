apply plugin: "war"

bootWar {
    mainClass = "com.codexd.aplv.AplvApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
